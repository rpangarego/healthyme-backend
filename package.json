{
  "name": "healthyme-backend",
  "version": "1.0.0",
  "description": "The backend of HealthyMe App, build with NodeJS, ExpressJS and MongoDB. This backend will response the request from frontend and return a response in JSON format.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "devStart": "nodemon server.js",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.15"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/mongoose": "^5.10.1"
  }
}
